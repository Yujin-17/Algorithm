class Solution {
    public int solution(String s) {
        int answer = 0;
        
        for(int i = 0; i < s.length(); i++){
            int a = Integer.parseInt(s);
            answer = a;
        }
        
        return answer;
    }
}